# Copy this file to `.env` and adjust as needed.
# Credentials are read from the environment so they are never hardcoded in the tests.

BASE_URL=https://opensource-demo.orangehrmlive.com
ADMIN_USERNAME=Admin
ADMIN_PASSWORD=admin123

# Set to "false" to watch the browser while the test runs.
HEADLESS=true
